{
  "name": "note-pad",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "dev": "npm-run-all --parallel nodemon browser-sync",
    "nodemon": "nodemon app.js",
    "browser-sync": "browser-sync start --proxy 'http://localhost:3000' --files 'public/**/*.{html,css,js}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "browser-sync": "^3.0.3",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "npm-run-all": "^4.1.5"
  }
}

